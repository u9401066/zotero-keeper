# =============================================================================
# Zotero Keeper - Environment Configuration
# =============================================================================
# Copy this file to .env and modify according to your setup.
# 將此檔案複製為 .env 並根據你的環境修改設定。

# -----------------------------------------------------------------------------
# Zotero Connection Settings | Zotero 連線設定
# -----------------------------------------------------------------------------

# Zotero machine IP or hostname
# If Zotero runs on the same machine, use localhost
# 如果 Zotero 在同一台機器上，使用 localhost
ZOTERO_HOST=localhost

# Zotero HTTP server port (default: 23119)
# This is Zotero's built-in port, usually no need to change
# 這是 Zotero 內建的 port，通常不需要修改
ZOTERO_PORT=23119

# Request timeout in seconds
# 請求超時時間（秒）
ZOTERO_TIMEOUT=30

# -----------------------------------------------------------------------------
# PubMed Integration (Optional) | PubMed 整合（選用）
# -----------------------------------------------------------------------------

# Your email for NCBI/PubMed API (recommended for higher rate limits)
# 你的 email，用於 NCBI/PubMed API（建議設定以獲得更高的請求限制）
NCBI_EMAIL=your.email@example.com

# NCBI API key (optional, for higher rate limits)
# Get from: https://www.ncbi.nlm.nih.gov/account/settings/
# NCBI API 金鑰（選用，可獲得更高的請求限制）
# NCBI_API_KEY=your_api_key_here

# -----------------------------------------------------------------------------
# Network Setup Notes | 網路設定說明
# -----------------------------------------------------------------------------
#
# Local Setup (Zotero on same machine):
#   ZOTERO_HOST=localhost
#   ZOTERO_PORT=23119
#
# Remote Setup (Zotero on different machine):
#   1. Enable Local API in Zotero (Run in Zotero console):
#      Zotero.Prefs.set("httpServer.localAPI.enabled", true)
#
#   2. On Windows, setup port proxy (Run as Administrator):
#      netsh interface portproxy add v4tov4 listenaddress=0.0.0.0 listenport=23119 connectaddress=127.0.0.1 connectport=23119
#
#   3. Add firewall rule (Run as Administrator):
#      netsh advfirewall firewall add rule name="Zotero HTTP Server" dir=in action=allow protocol=TCP localport=23119
#
#   4. Set ZOTERO_HOST to your Zotero machine's IP
#      ZOTERO_HOST=192.168.x.x
#
# -----------------------------------------------------------------------------
