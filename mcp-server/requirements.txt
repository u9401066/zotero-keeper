# Requirements for zotero-keeper MCP Server
# Generated from pyproject.toml
#
# Install with uv (required):
#   uv pip install -r requirements.txt
#
# Or install from pyproject.toml directly (recommended):
#   uv sync

# Core dependencies
mcp>=1.0.0
httpx>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
rapidfuzz>=3.0.0

# Optional: PubMed integration (uncomment to enable)
# pubmed-search-mcp>=0.3.8
