# Requirements for zotero-keeper MCP Server
# Generated from pyproject.toml - for users who prefer pip
# 
# Recommended: Use uv instead (faster)
#   uv pip install -e .
#
# Or with pip:
#   pip install -r requirements.txt

# Core dependencies
mcp>=1.0.0
httpx>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
rapidfuzz>=3.0.0

# Optional: PubMed integration (uncomment to enable)
# pubmed-search-mcp>=0.1.8
